{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}




 <div class="row d-flex justify-content-center">
    <div class="col-md-6">
        <div class="card p-3 mb-5 rounded mt-5">
            <article class="card-body">
            <h4 class="card-title mb-4 mt-1">Demande accepetée, veuiller fournir les dernières informations</h4>
                 {{ form_start(form) }}

                {{ form_row(form.telBanque) }}
                {{ form_row(form.rib) }}
            
                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">Finir</button>
                </div>
            {{ form_end(form) }}
            </article>
        </div> 
    </div>
</div>

{% endblock %}